不推翻各步 Go。下列是过程/读图上的小问题，修正文或图时对着改。

## step 14

2. **内层 git 还没跟上自己写的合同。** ignore 规则对：结果不进库、主图 pdf/png 可进库。本地 `dff58aa` 已跟踪 `README.md`、`src/io_results.py`、`scripts/compact_results.py` 以及 Hsieh / horizon / \(L_F\) / \(V_T\) 入口，但 `origin/master` 仍停在 `358cf7c`，远端没有 Step 14 入口。克隆内层仓库复现不了 README 里的命令；需要 `git push`。
