对照：当前 `note/sections/experiments.tex`、`experiment/` 代码与三张主图（shifted saddle，\(w_1=0\)）。旧版里「手工改第一步 action」等条目已过时，本文按现状重写。

标尺要分开：
- **A. 对当前正文的弱声称**（Numerical Illustrations，不宣称优于已有算法）；
- **B. 对更强的经验叙事**（unknown-\(L_F\) 系统适应、\(V_T\) 插值、相对 Hsieh 的算法优势）。

A 已基本站得住；B 中 Hsieh、\(L_F\) sweep 与 \(V_T\) 插值已落地。下面「该修的图」服务 A；**建立 Hsieh baseline** 与 \(L_F\)/\(V_T\) sweep 服务 B。

------------------------------------------------------------------------

1. **总体结论**

   * 代码实现与 D005 / `thm:t006` 总体一致；更新顺序、warm 不重置、clipping、\(\gamma\) 冻结、指标定义基本正确。
   * 初始化已经干净：投稿实例为 \((a,b)=(0.4 e_1,0.4 e_2)\)，脚本禁止覆盖 `action`，并断言 \(\|w_1\|=0\) 且第 2 步离开原点。
   * 作为理论论文的 **Numerical Illustrations**，三张主图加附录已对齐正文弱声称（self-play 平台、有限加倍、\(V=0\) fallback、same-run 不重置、unknown-\(L_F\) 与 \(V_T\) 指向附录）。
   * Hsieh 对照已经接到 G3 / switch，但 G3 上**双方都饱和**，不能当成 realized-regret 优势。Step 18 已把这条写进正文第四段与 limitations。
   * 主文读图上更急的 **Exp.2 负 regret / Exp.3 对鞍点累计** 已由 Step 11–12 修掉。Hsieh OptDA 已作为 literature baseline 接入 Figure 2 右（\(\tau=1\)，未拟合）。
   * 因此：插图体裁已对齐、comparator / 纵轴已修好、Hsieh 实现通过 G2 self-play 平台体检。经验 BoBW 优势仍未成立，因为 G3 上 Hsieh 也饱和。`PLAN.md` 完成判据表已勾满。

2. **目前做得比较好的地方**

   * 更新顺序、doubling、warm rescaling、clipping、\(\gamma\) 固定；G1/G2 self-play 的 comparator 已是 `game.saddle()`。
   * 有 SPEC、不变量、smoke（\(w_1\)、\(J\ge 1\) 后半冻结、G3 的 \(V\equiv 0\) 与 \(G_T\in[1,3]\)）。过程严谨性高于「只有几条曲线」。
   * **Exp.1 / Thm.t006.B：** G2 上 regret 平台约 5，\(Q_t^{\mathrm{obs}}\) 冻在约 0.029，restricted gap 与 \(1/t\) 同形。G1 identity 的 regret 恒 0 是 \(\Phi\equiv 0\) 的结构退化，正文已承认；\(T=2\times 10^5\) 后 \(Q\to 0.343\)。谱归一化 Gaussian 附录补上非平凡 G1 regret。
   * **Exp.2-right / Thm.t006.C：** G3 很干净：\(V_T(u^\star)=0\)，\(G_T=\Theta(1)\)，regret 平台约 80，无 \(\sqrt{t}\) 趋势。
   * **Exp.3 机制：** \(J=1\) 后冻结；restart 在 \(t=3\) 回原点再爬向 \(\|x^\star\|\approx 5\)；对 \(x^\star\) 的 regret 饱和（warm \(\approx 77\)，restart \(\approx 171\)，常数偏移）；正文不声称 \(\sqrt{K}\) 税。作为 ablation 方向正确。
   * **Exp.2-left / Thm.t006.D：** 连续 slow switch；主曲线为 \(\|x_t-a\|\)、\(\|y_t-b\|\) 与 \(V_t^x(a)\)（负 \(\mathrm{Reg}^x(a)\) 不再当主曲线）。切换后距离有界，\(V\) 在竖线后升起，\(\gamma\) 不重置。
   * 正文表述与实验匹配：G3 已叠加 Hsieh OptDA；双方饱和，比较仍限制为 theorem statements plus 如实报告。第 6 节已点明这一点。

   G3 的 claim 边界（必须写进评价，避免过读）：

   * 它分离的是 **theorem 上界 vs theorem 上界**（\(\widetilde O(1)\) vs \(O(G\sqrt{T})\)），不是旧算法真实 regret 的 \(\Omega(\sqrt{T})\)。
   * \(y_t\equiv 1\) 使诱导损失完全 stationary，故对一切固定 \(u\) 都有 \(V_T(u)=0\)，例子偏「容易」。
   * 作为投稿用 separation：**够用**。17b 补了非平稳且 \(V_T(u^\star)=O(1)\) 的路径；Hsieh 在该路径上跟踪，静态 regret 为负，仍**不能**改口 realized \(\Omega(\sqrt{T})\)。

3. **目前最主要的问题**（按对当前主文的伤害排序）

   * **Exp.3 comparator 选错：** **已由 Step 11 修掉。** vs \(y\equiv e_1\) 现对诱导最优 \(x^\star\) 累计，\(V_T(x^\star)=0\)，两曲线饱和后差一个常数。
   * **Exp.2-left 负 regret 被误当成 fallback 图：** **已由 Step 12 修掉。** 主曲线改为距离与 \(V_t^x(a)\)；协议仍是连续慢正弦；claim 只讲 part D。
   * **unknown-\(L_F\) 只有存在性：** **已由 Step 16 补 \(A=cI\) sweep（禁止谱归一化）。** \(J=1\) 对所有 \(c\)；\(\ell_T=\sqrt{2}\,L_F\) 是一次加倍恒等。大 \(c\) 上 \(Q\) 立即冻住；\(c=4\) 的 regret 于 \(T=10^5\) 冻在 \(80.76\)，\(c=8\) 仍在靠近（\(\|x_T-a\|=0.033\)），不是 \(\sqrt{T}\)，也还不是 late platform。Gaussian 附录仍然不是 \(L_F\) sweep。
   * **\(V_T(u)\) 只有 \(V=0\) 端点：** **已由 Step 17 补。** 17a：G2 正弦幅度扫，\(\mathrm{LinReg}\) 随 \(\sqrt{V}\) 在 \(\eta\le 0.4\) 上升，更大 \(\eta\) 开始跟踪。17b：G3 非平稳 \(T_{\mathrm{per}}=\sqrt{T}\)，终端 \(V_T\approx 4.93=O(1)\)（单条 \(V_t\) 仍随 \(t\) 涨），路径长 \(\approx 283\)，D005 终端约 \(58.5\)（峰值 \(61.6\) 后缓降）。Hsieh 同路径跟踪至 \(-986\)，禁止写成 \(\Omega(\sqrt{T})\)。G2（\(A=I\)）与 G3 的 \(V\) 都不依赖 \(u\)，不是 comparator-local \(u\) 依赖性的实验。
   * **rate 主要靠单条曲线和参考斜率：** **已由 Step 15 补终端表。** G2 的 \(\mathrm{Reg},Q\) 从 \(T=5\times10^3\) 起冻结；G1 的 \(Q\) 相对短窗已进入平台（\(0.3425\to 0.3433\) 仍有残差）。G2 上 \(t\cdot\mathrm{Gap}_t\) 随 \(t\) 加倍而减半是本实例 \(1/T^2\)，与 \(O(1/T)\) 上界数量级相容，不另声称指数，也不是 Figure 1 虚线斜率。G1 中行标 \(t=99991\)。
   * **没有 literature baseline：** **已由 Step 13 落实 Hsieh OptDA。** G2 self-play 平台（实现有效）。G3 上 D005 \(\approx 80\)、Hsieh \(\approx 1.2\)，**双方饱和**；switch 上两条距离都有界。禁止把这解读成 realized \(\Omega(\sqrt{T})\)，也禁止把 self-play 两平台并排当 BoBW。
   * **闭式贡献没有实验。** `check_closedform.py` 只验证 `radial_q` 可逆与数值稳定，并不独立求解 FTRL argmin。\(O(d)\) 是理论卖点，实验空白；可保持省略，但不要写成「已经对拍过闭式更新」。
   * **复现性：内部合同齐，对外入口已补。** `SPEC.md`、`PLAN.md`、`configs/default.yaml`、`experiment/README.md`。新 run 写 compact summary + npz；旧的数 MB 全程 json 已剥离。外层仓库仍 gitignore 整个 `experiment/`。

4. **不要再列为待办**

   * Exp.1 改为 shifted-saddle、严格 \(w_1=0\)：**已完成**（含正文与 Gaussian 附录）。
   * 把 warm vs restart 误写成 literature baseline：继续当 ablation；literature baseline 由 Step 13 落实 Hsieh。
   * 把 Exp.2-left 升级成不连续压力测试：不采纳。Fallback 走 G3 + Hsieh，以及修好 comparator 后的 Exp.3 vs-const。

5. **必须全部落实的条目**（= `PLAN.md` Step 11–18，**已全部落实**）

   1. Exp.3：vs-const 的 comparator 改为诱导最优 \(x^\star\)。
   2. Exp.2-left：换纵轴 / 补 \(V_t\) 或距离，去掉大负 regret 作为主曲线。
   3. 建立 Hsieh (2021) baseline：G3 主对照，switch 次之；self-play 只作实现体检。
   4. README + compact summary json / npz。
   5. Multi-horizon 终端表。
   6. \(L_F\) sweep（\(A=cA_0\)，禁止谱归一化）。
   7. \(V_T\) 幅度扫频（17a）**以及**非平稳且 \(V(u^\star)=O(1)\) 的更强分离（17b）。
   8. 改 `experiments.tex` / 附录，删除 “no other algorithm is shown”。

6. **论文在 Step 18 完成后可以怎么表述**

   修完 Step 11–12 但未做 Hsieh 时，仍只能写 illustrations，且必须删掉大负 regret 主曲线。

   修完 Step 13 且 G3 对照方向正确之后，可以写 D005 vs Hsieh 的真实 regret，并删除 “no other algorithm is shown”。在此之前比较仍应限制为 theorem statements。

   修完 Step 16–17 之后，附录可以写 finite doubling tracks realized smoothness，以及 linearized regret 随 \(\sqrt{V_T}\) 变化（本实例 \(V\) 不依赖 \(u\)）。

   全程不建议写未经对照支撑的 “comprehensively validate superiority”。若 G3 上 Hsieh 也饱和，必须写入 limitations。

7. **最终评级**

   * 实现正确性：**约 4.5/5**（初始化那条删除；闭式对拍不是独立 FTRL 验证）。
   * theorem illustration：**约 4.4/5**（B 完整，C 有干净端点，D 有 same-run，A 有 \(A=cI\) 尺度扫、路径上仍是 \(J=1\)）。
   * 实验过程严谨性：**偏高**（SPEC / 不变量 / smoke）。短板在设计和读图，不在工程草率。
   * 实验全面性：**PLAN 完成判据表已齐。**
   * 对当前正文弱声称：**已能支撑。**
   * 对 BoBW superiority 的**经验证据**：**约 3/5**（有第二条算法曲线，但 G3 上 Hsieh 也饱和，证据仍主要是上界分离）。
   * 作为 Numerical Illustrations：**comparator / 纵轴 / Hsieh 对照 / horizon 表 / \(L_F\) sweep / \(V_T\) 已接到正文或附录。**
   * 作为带对照的 ICLR 实验节：**Hsieh 已实现且通过 self-play 体检；G3 不能写成 realized-regret 优势。**

最关键的一句话：

**`PLAN.md` 完成判据表已勾满。实验说明实现正确，以及 self-play / 有限加倍 / \(V=0\) fallback / same-run 不重置 / unknown-\(L_F\) / \(V_T\) 这些现象会出现，并且有一条对拍过的 Hsieh OptDA 曲线。G3 上双方都饱和，相对 Hsieh 的算法优势仍然只在 theorem statements。不得声称 comprehensively validate superiority。**

------------------------------------------------------------------------
8. **落地步骤**

执行合同以 **`PLAN.md` §6 与 `SPEC.md` 为准**。Step 11–18 **已全部完成**。

**Step 11.（已完成）** Exp.3 vs-const comparator \(\to x^\star=a-A(e_1-b)/\mu\)。\(\mathrm{Reg}^x(x^\star)\) 饱和。改 Figure 3。

**Step 12.（已完成）** Exp.2-left 主曲线改为 \(\|x_t-a\|\)、\(\|y_t-b\|\)、\(V_t^x(a)\)。协议（连续 slow switch）不变。

**Step 13.（已完成）** `src/hsieh.py`：G3 主对照、G2 switch、G2 self-play 体检。G3 双方饱和，已写入 limitations。Restart 不是 baseline。

**Step 14.（已完成）** `experiment/README.md` + compact summary json / npz。

**Step 15.（已完成）** Multi-horizon 表（G2 与 G1 \(Q\) checkpoint），进附录。

**Step 16.（已完成）** \(L_F\) sweep：\(A=cI\)，\(c\in\{0.25,0.5,1,2,4,8\}\)，禁止谱归一化。

**Step 17.（已完成）** \(V_T\)：17a 幅度扫频与 17b 非平稳 \(V(u^\star)=O(1)\) 都做。

**Step 18.（已完成）** 改 `experiments.tex` / `appendix_experiments.tex`，覆盖第 5 节全部条目。

`PLAN.md` 完成判据表已勾满。不得把 G3 写成 realized-regret 优势。
